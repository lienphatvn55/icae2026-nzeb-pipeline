# PI-HGAT NZEB pipeline — ICAE2026. Verified working environment.
# Python 3.13.3, Windows 11 + CUDA 12.4 (training is GPU-accelerated but runs on CPU too).
#
# PyTorch + PyG must match your CUDA build. Install torch first, e.g.:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install torch-geometric==2.7.0
# then the rest below.

torch==2.6.0
torch-geometric==2.7.0
pymoo==0.6.1.6
xgboost==3.0.2
scikit-learn==1.8.0
pandas==2.2.3
numpy==2.2.6
matplotlib==3.10.1
networkx==3.6.1
shap==0.48.0
openpyxl==3.1.5
nbformat>=5.10
nbconvert>=7.16
